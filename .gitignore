node_modules/
.DS_Store
.env
.env.*
!.env.example
!.env.cloud.example
dist/
.vercel/

# Prompts generados por pnpm mapping:prompt (regenerables)
docs/feature-specs/generated/

# Supabase CLI (local); no versionar — cada copia cliente debe tener estado propio
.supabase/

# Dumps / backups SQL locales — NUNCA versionar (pueden traer hashes de password y datos)
dump*.sql
**/dump_*.sql
*.dump
*.sql.gz
*-backup.sql
*_backup.sql
