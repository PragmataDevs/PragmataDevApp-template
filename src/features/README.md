# `src/features/`

Cada dominio: **`pages/` · `hooks/` · `components/` · `types/`** (+ `navigation.ts` opcional, subfeatures anidadas).

Documentación: **`docs/erp-features-structure.md`** · índice: **`docs/README.md`**.

Modelos de negocio aquí en `types/`, no en `src/types/` (solo `src/types/core/base.ts`).
