{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --dir astro run build",
  "rewrites": [{ "source": "/favicon.ico", "destination": "/favicon.svg" }]
}
